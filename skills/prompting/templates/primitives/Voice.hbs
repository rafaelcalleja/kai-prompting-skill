{{!--
  VOICE Template
  Purpose: Generate voice/personality configuration for agents or TTS
  Data Structure:
    voice:
      title: string
      description: string (optional)
    agents:
      agent_id:
        name: string
        voice_id: string
        voice_name: string
        rate_wpm: number
        stability: number
        similarity_boost: number
    presets:
      preset_name:
        stability: number
        similarity_boost: number
        description: string
--}}

# {{voice.title}}

{{#if voice.description}}
{{voice.description}}

{{/if}}
---

## Agent Voice Configurations

{{#each agents}}
### {{name}}

| Parameter | Value |
|-----------|-------|
| Voice ID | `{{voice_id}}` |
| Voice Name | {{voice_name}} |
| Rate (WPM) | {{rate_wpm}} |
| Stability | {{stability}} |
| Similarity Boost | {{similarity_boost}} |

{{/each}}

{{#if presets}}
---

## Voice Presets

{{#each presets}}
### {{@key}}

{{#if this.description}}
{{this.description}}

{{/if}}
| Parameter | Value |
|-----------|-------|
| Stability | {{this.stability}} |
| Similarity Boost | {{this.similarity_boost}} |

{{/each}}
{{/if}}
