{{!--
  STRUCTURE Template
  Purpose: Generate standardized multi-step execution patterns
  Data Structure:
    workflow:
      name: string
      description: string
      timing: string (optional)
    phases:
      - name: string
        purpose: string
        duration: string (optional)
        steps:
          - action: string
            instructions: string
            tools: [string] (optional)
            output: string (optional)
--}}

# {{workflow.name}}

{{workflow.description}}

{{#if workflow.timing}}
**Estimated Duration:** {{workflow.timing}}
{{/if}}

---

{{#each phases}}
## Phase {{@index}}: {{name}}

**Purpose:** {{purpose}}

{{#if duration}}
**Duration:** {{duration}}
{{/if}}

### Steps

{{#each steps}}
#### {{@../index}}.{{@index}} {{action}}

{{instructions}}

{{#if tools}}
**Tools:** {{#each tools}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}

{{#if output}}
**Expected Output:** {{output}}
{{/if}}

{{/each}}

{{#unless @last}}
---

{{/unless}}
{{/each}}
